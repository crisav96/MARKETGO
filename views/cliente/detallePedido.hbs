<section class="detalle-pedido">

    <a href="/ordenes/historial" class="volver">
        <i class="bi bi-arrow-left"></i> Volver a mis pedidos
    </a>

    <div class="detalle-cabecera">

        <h2>Pedido #{{orden.id}}</h2>

        <span class="badge-estado badge-{{orden.estadoClase}}" data-badge-estado data-orden-id="{{orden.id}}">
            {{orden.estado}}
        </span>

    </div>

    <div class="detalle-grid">

        <div class="detalle-productos">

            <h3>Productos</h3>

            {{#each orden.productos}}

                <div class="detalle-item">

                    <img
                        src="/img/productos/{{imagen}}"
                        alt="{{nombre}}"
                        onerror="this.onerror=null; this.src='/img/productos/default.png';">

                    <div class="detalle-item-info">

                        <span>{{nombre}}</span>

                        <small>{{cantidad}} x ₡{{precio}}</small>

                    </div>

                    <strong>₡{{subtotal}}</strong>

                </div>

            {{/each}}

        </div>

        <aside class="detalle-resumen">

            <h3>Resumen</h3>

            <div class="detalle-dato">
                <span>Fecha</span>
                <strong>{{orden.fechaFormateada}}</strong>
            </div>

            <div class="detalle-dato">
                <span>Dirección</span>
                <strong>{{orden.direccion}}</strong>
            </div>

            <div class="detalle-dato">
                <span>Teléfono</span>
                <strong>{{orden.telefono}}</strong>
            </div>

            <div class="detalle-dato">
                <span>Método de pago</span>
                <strong>{{orden.metodo_pago}}</strong>
            </div>

            <div class="detalle-total">
                <span>Total</span>
                <strong>₡{{orden.total}}</strong>
            </div>

        </aside>

    </div>

</section>
